<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hz.fcloud.dao.EquipmentMapper">
    <select id="findAll" resultType="cn.hz.fcloud.entity.Equipment">
        select * from equipment
    </select>
    <select id="findOne" parameterType="int" resultType="cn.hz.fcloud.entity.Equipment">
        select * from equipment where code=#{code}
    </select>
    <insert id="addEquipment" parameterType="cn.hz.fcloud.entity.Equipment">
        insert into equipment values(#{id,jdbcType=BIGINT},#{code},#{name},#{type},#{province},#{city},#{district},#{address},#{lng},#{lat},#{create_user},#{create_time},#{is_deleted},#{company_id},#{remark})
    </insert>
    <update id="delEquipment" parameterType="int">
        update equipment set is_deleted=1 where code=#{code}
    </update>
</mapper>